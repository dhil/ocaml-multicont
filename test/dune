;; Copy examples
(copy_files# ../examples/*.ml)

(tests
 (names async
        choice
        generic_count
        nqueens
        supervised)
 (action
   (bash "env LD_LIBRARY_PATH=. ../examples/%{test}"))
 (libraries multicont))
